<UserControl x:Class="Streaming_Muesic_WPF.Process.ColorOnVolume.ColorOnVolumeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="250">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="2.5*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>

        <Label Content="Sensitivity" VerticalAlignment="Center"/>

        <Grid Grid.Column="1">
            <ProgressBar Height="24" Maximum="1" 
                         SmallChange="0.001" Value="{Binding VuMix}"/>
            <Slider VerticalAlignment="Center"
                    Maximum="1.0" Value="0.5" />
        </Grid>

    </Grid>
</UserControl>
